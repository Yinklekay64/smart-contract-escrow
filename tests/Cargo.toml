[package]
name = "escrow-integration-tests"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
path = "src/lib.rs"

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
